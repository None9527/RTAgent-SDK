# RTAgent Docs Index

| Path | Purpose | Read When | Owner | Update Trigger | Validation |
|---|---|---|---|---|---|
| `README.md` | Public SDK entrypoint with quick start, host resume demo, provider setup, validation commands, and boundary notes. | First-time SDK onboarding, release readiness checks, or validating public usage shape. | Runtime/SDK owner | Public facade, examples, provider setup, validation commands, or release boundary changes. | `go test ./...`, `go vet ./...`, and documented example commands |
| `docs/project-structure.md` | RTAgent repository/module map and SDK package ownership boundaries. | Locating runtime SDK modules or changing package, example, internal wiring, or docs structure. | Runtime/SDK owner | Public SDK files, runtime wiring, examples, docs directories, or top-level commands change. | `go test ./...` and `go vet ./...` |
| `docs/architecture/sdk-core.md` | Public SDK facade and interface boundary. | Planning or changing the embeddable SDK API. | Runtime/SDK owner | SDK public types, facade methods, runtime contracts, or adapter boundaries change. | `go test ./...` and `go vet ./...` |
| `docs/architecture/ngoagent-current-sdk-interface-analysis.md` | Source-backed analysis of current ngoagent architecture and SDK-driven core interfaces. | Replanning RTAgent SDK from ngoagent source instead of old architecture docs. | Runtime/SDK owner | ngoagent runtime/app/host/agent/controlplane boundaries change. | Recheck cited ngoagent source paths and run `go test ./...` |
| `docs/release/v1-readiness.md` | v1 release readiness gates, non-goals, validation commands, and stop criteria. | Deciding whether RTAgent Runtime SDK can be labeled v1.0 or planning remaining release work. | Runtime/SDK owner | v1 gate status, module path, release naming, validation commands, public compatibility policy, or README status changes. | Commands listed in the document's Validation section |
| `docs/release/release-process.md` | v1 release process, module-path/naming steps, validation/preflight flow, and packaging blockers. | Preparing a v1 release branch, tag, or external repository publication. | Runtime/SDK owner | Final module path, release naming, README status, validation scripts, or packaging policy changes. | `bash scripts/validate_sdk.sh` and `bash scripts/release_preflight.sh` |
| `scripts/validate_sdk.sh` | Local SDK release-candidate validation script covering package docs, recursive shell syntax checks, final module-path validation cases, tests, vet, smoke command, examples, and host resume demo with default temp database cleanup. | Before claiming SDK release readiness or preparing a release branch/tag. | Runtime/SDK owner | Validation command set, examples, package docs, module-path validation rules, temp artifact policy, or release readiness gates change. | `bash scripts/validate_sdk.sh` |
| `scripts/release_preflight.sh` | Release gate script that blocks local or invalid module paths, dirty worktree, tracked or root-level generated artifacts, package-doc policy drift, public API snapshot drift, SDK boundary drift, SDK shape/docs/examples drift, and README/v1-readiness/module-path identity mismatch before v1. | Before tagging, packaging, or publishing the SDK. | Runtime/SDK owner | Module path, packaging rules, README or v1-readiness status, public API snapshot, or public compatibility docs change. | `bash scripts/release_preflight.sh` |
| `scripts/set_module_path.sh` | Mechanical helper for checking, dry-running, and applying `go.mod` plus Go import changes from the local module path to the final repository import path. | After a candidate final module path is chosen and before v1 release validation. | Runtime/SDK owner | Module path validation, migration behavior, dry-run safety, or package layout changes. | `bash -n scripts/set_module_path.sh`, `bash scripts/set_module_path.sh --check <final module path>`, `bash scripts/set_module_path.sh --dry-run <final module path>`, then `bash scripts/validate_sdk.sh` |
| `scripts/lib/module_path.sh` | Shared final Go module import-path validation helper used by release, migration, and SDK validation scripts. | Changing module-path validation rules or reviewing release identity gates. | Runtime/SDK owner | Final module path validation rules change. | `bash -n scripts/lib/module_path.sh`, `bash scripts/set_module_path.sh --dry-run <final module path>`, `bash scripts/validate_sdk.sh`, and `bash scripts/release_preflight.sh` |
| `docs/api/public-api.snapshot.txt` | Normalized `go doc -all ./pkg/rtagent` public API snapshot used to detect accidental exported surface, field, and method drift. | Reviewing exported SDK API changes or release readiness. | Runtime/SDK owner | Intentional exported SDK symbol/package-doc change. | `bash scripts/check_public_api_snapshot.sh` |
| `scripts/check_public_api_snapshot.sh` | Compares current public SDK API docs against `docs/api/public-api.snapshot.txt`. | Before merging public SDK API changes or release validation. | Runtime/SDK owner | Snapshot format or public API check behavior changes. | `bash scripts/check_public_api_snapshot.sh` |
| `scripts/audit_sdk_boundary.sh` | Audits SDK v1 boundary invariants such as single ModelProvider contract, no legacy Execute entry, no legacy StreamingModelProvider, no exposed startup/persistence types, confined startup container imports, confined aggregate persistence bundle usage, no unused dataset-export/audit persistence surface, and removed product-shell files. | Before claiming SDK boundary readiness, release validation, or v1 preflight. | Runtime/SDK owner | Public facade, model provider contract, startup/persistence boundary, persistence surface, product-shell removal, or release gates change. | `bash scripts/audit_sdk_boundary.sh` |
| `scripts/audit_sdk_shape.sh` | Audits SDK package shape so `pkg/rtagent`, internal runtime components, internal persistence domain contracts, and SQLite adapters do not regress into oversized source files or the old `types.go` monolith. | Before release validation, after major refactors, or when adding large SDK source/test/runtime/domain/adapter files. | Runtime/SDK owner | File-size budgets, package split policy, public contract file organization, runtime split policy, persistence domain split policy, persistence adapter split policy, or release validation gates change. | `bash scripts/audit_sdk_shape.sh` |
| `scripts/audit_sdk_docs.sh` | Audits docs index integrity and required durable-doc metadata headings. | Before release validation, after docs changes, or when adding/removing durable project docs. | Runtime/SDK owner | Docs index, durable docs, release docs, or project memory structure changes. | `bash scripts/audit_sdk_docs.sh` |
| `scripts/audit_sdk_examples.sh` | Audits SDK example and smoke command entrypoint style, explicit runtime config usage, local example/command validation coverage, external-provider opt-in documentation, temp database cleanup policy, and no internal imports from host-facing commands/examples. | Before release validation, after adding/changing examples or commands, or when changing validation coverage or temp artifact policy. | Runtime/SDK owner | Examples, commands, validation coverage, external-provider example policy, temp artifact policy, or release gates change. | `bash scripts/audit_sdk_examples.sh` |
| `scripts/update_public_api_snapshot.sh` | Regenerates the public API snapshot after an intentional compatible API change. | After reviewing and accepting a public SDK API change. | Runtime/SDK owner | Snapshot generation behavior or accepted public API changes. | `bash scripts/update_public_api_snapshot.sh` then `bash scripts/check_public_api_snapshot.sh` |
| `docs/api/model-providers.md` | ModelProvider API, OpenAI-compatible provider, DashScope integration, and retry/failure semantics contract. | Adding or changing model providers or SDK integration tests. | Runtime/SDK owner | ModelProvider, OpenAI-compatible request mapping, DashScope config, retry/failure (no-retry) semantics, or integration test changes. | `go test ./...`, `go vet ./...`, optional DashScope integration test |
| `docs/api/public-compatibility.md` | Public `pkg/rtagent` v1 compatibility policy, stable surface, additive changes, and non-contract internals. | Reviewing exported SDK API changes or deciding whether RTAgent can be tagged v1.0. | Runtime/SDK owner | Exported SDK symbols, event schema, WorldState schema, provider/permission contracts, concurrency contract, module path, or release boundary changes. | `go doc -all ./pkg/rtagent`, `bash scripts/check_public_api_snapshot.sh`, `go test ./...`, and `go vet ./...` |
| `docs/api/tool-providers.md` | ToolProvider and ToolRegistry API, multi-provider config, and tool dispatch behavior. | Adding/changing SDK tool providers, registry wiring, or tool dispatch behavior. | Runtime/SDK owner | ToolProvider, ToolRegistry, ToolSpec, ToolCall, runtime tool config, or schema snapshot behavior changes. | `go test ./...` and `go vet ./...` |
| `docs/api/world-state.md` | WorldState public schema, partitions, handles, projection sources, determinism contract, and tool/capability read model. | Changing WorldState contracts, projection sources, determinism semantics, or tool/MCP/skill capability state. | Runtime/SDK owner | `WorldStateSnapshot`, partitions, runtime fact sources, projection determinism, context packet, tool schema, permission, activity, task, memory, MCP, or skill projection behavior changes. | `go test ./pkg/rtagent -run TestRuntimeWorldState -count=1`, `go test ./...`, and `go vet ./...` |
| `docs/api/permission-center.md` | PermissionCenter API, approval decisions, grant scopes, and tool/workspace gates. | Changing approval, permission, grant, tool execution, workspace mutation, or host approval wiring. | Runtime/SDK owner | PermissionCenter public contracts, policy modes, persistence, or approval resume behavior changes. | `go test ./...` and `go vet ./...` |
| `docs/api/session-lifecycle.md` | Session lifecycle API, run graph, StopSession behavior, and external resume feasibility. | Changing session/run lifecycle, run graph, StopSession, approval resume, or CLI/frontend resume behavior. | Runtime/SDK owner | Session public contracts, run graph persistence, stop modes, or resume semantics change. | `go test ./...` and `go vet ./...` |
| `examples/` | Runnable host integration examples for minimal runtime, DashScope, approval resume, CLI resume, and MCP/Skill inventory. Local non-persistent examples clean up temporary runtime storage after execution. | Embedding the SDK or checking expected host integration shape. | Runtime/SDK owner | Public facade, providers, resume, inventory contracts, temp artifact policy, or example coverage changes. | `go test ./...` and `bash scripts/audit_sdk_examples.sh` |
| `docs/sdk-handbook.md` | Consolidated SDK operation handbook: conceptual model, quick start, configuration, full API reference, run loop, event journal, WorldState, permission, session, checkpoint, tentacles, orchestration, convergence, context budget, model provider, concurrency, compatibility, validation, glossary. | First-time embedding, API lookup, contributor onboarding, or reviewing design principles in one place. | Runtime/SDK owner | Public API, contracts, design principles, convergence, context budget, WorldState cache, tentacle types, orchestration patterns, or release identity changes. | `go test ./...`, `go vet ./...`, `bash scripts/validate_sdk.sh`, `bash scripts/audit_sdk_docs.sh` |
